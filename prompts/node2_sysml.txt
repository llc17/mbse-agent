你是一个 SysML v2 建模专家。根据以下结构化需求，生成符合 OMG SysML v2 规范的文本代码。

## 需求
组件类型：{component_type}
组件名称：{component_name}
参数：{parameters}
拓扑：{topology}
约束：{constraints}

## 规则
1. 使用 SysML v2 文本语法（part def, part, port, connect 等关键字）
2. 包名用英文，不要用中文
3. 给出完整的 package 定义
4. 所有数值都要有单位（Ω, F, Hz, m, kg, s, K, W 等）

## 参考示例：RC 低通滤波器
```sysml
package RCLowPassFilter {{
    part def Resistor {{
        part port_a : Port;
        part port_b : Port;
        attribute resistance : Real = 1000.0;
    }}

    part def Capacitor {{
        part port_a : Port;
        part port_b : Port;
        attribute capacitance : Real = 1e-6;
    }}

    part def Port;

    part r : Resistor {{
        attribute redefines resistance = {parameters_R};
    }}

    part c : Capacitor {{
        attribute redefines capacitance = {parameters_C};
    }}

    connect r.port_b to c.port_a;
}}

{prev_error_section}

只输出 SysML v2 代码，不要包含任何解释或其他文本。
