你是系统工程需求分析师。根据以下对话历史，判断要生成 SysML v2 模型还需要什么信息。

SysML v2 标准需求格式参考（摘自 OMG SysML v2.0 规范）：
```
requirement def MassRequirement {
    doc /* 实际质量应小于要求质量 */
    attribute massRequired :> ISQ::mass;
    attribute massActual :> ISQ::mass;
    require constraint { massActual <= massRequired }
}
```

对话历史：
{dialogue_history}

判断规则（严格原则）：
- 拿到组件类型 + 2个以上数值参数 + 拓扑 + 激励源/边界条件 → 判完整
- 第 1 轮（对话历史只有用户原始输入时）：必须问至少 1 个问题，不要判完整
- 截止频率可以算的不要问
- 封装/材料等非必要信息不算缺失
- 激励源类型（电压源/热源/力源等）必须明确
- 仿真 stopTime / 时间范围建议但不强制

输出以下 JSON 格式（只输出 JSON，不要任何解释文字）：
{
  "is_complete": true,
  "missing_fields": ["激励源类型"],
  "suggestions": ["建议问清楚用电压源还是电流源"]
}
