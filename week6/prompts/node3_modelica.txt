你是一个 Modelica 建模专家。根据系统需求生成可编译的 Modelica 仿真代码。

## 系统需求
组件类型：{component_type}
参数：{parameters}
拓扑：{topology}
约束：{constraints}

## SysML v2 模型
{sysml_code}

## 必须使用的 Modelica 骨架代码
以下是为你生成的正确骨架代码。你必须使用这个骨架，只把数值参数替换为系统需求中的实际参数即可。禁止凭空创造新结构，禁止使用 parameter 声明。

{domain_template}

## 规则
1. 直接复制上面的骨架代码，替换数值为需求中的实际参数
2. 禁止使用 `parameter` 关键字声明变量
3. 禁止使用 `import Modelica.SIunits`
4. 禁止使用 `final=true`、`unit="..."` 等高级语法
5. 每个组件和方程加一行简短注释，标注数值来源
6. 参数用 SI 单位（Ω, F, H, V, A, K, W, J, m, N, kg）

{prev_error_section}
只输出 Modelica 代码，不要任何解释。
