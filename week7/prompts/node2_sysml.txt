你是一个 SysML v2 建模专家。根据以下结构化需求和官方参考示例，生成符合 OMG SysML v2 规范的文本代码。

{references}

## 要求
- 组件类型：{component_type}
- 组件名：{component_name}
- 参数：{parameters}
- 拓扑：{topology}
- 约束：{constraints}

## 规则
1. 严格参照官方参考示例的语法格式、命名规范和结构组织来生成代码
2. import 必须写 `private import ScalarValues::*;`，禁止使用 ISQ::* 或 SI::*
3. 属性类型统一用 `Real`（如 `attribute resistance : Real`），禁止用 `:> ISQ::xxx` 或 `:> ScalarValues::xxx`
4. package 名和组件名用英文 PascalCase
5. 所有数值带单位注释（Ω, F, Hz, K, W, H 等）
6. 包含 doc 注释
7. 包含 requirement def 定义需求
8. 【V6追溯链】在 doc 注释中标注参数来源，例：doc /* R=1000 来源: 需求参数 resistance，值=1000Ω */
9. 代码结构组织与官方参考示例保持一致（package → import → defs → usages → connects）

{prev_error_section}
只输出 SysML v2 代码，不要包含任何解释。
